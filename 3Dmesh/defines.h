#include <opencv\cv.h>
#include <opencv\highgui.h>
#include <stdio.h>
using namespace std;
using namespace cv;


#define ABS(x) (((x) >= 0) ? (x) : (-(x)))

#define MESHNUMBER 1
#define MESHRIGIDITY 1.0   //larger, more rigid
